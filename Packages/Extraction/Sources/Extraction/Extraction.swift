/// Call-type routing of extracted context into repo artifacts; diff-safe,
/// confirmation-gated write-back.
public enum ExtractionModule {
    /// Module identity used in diagnostics and privacy-safe logs.
    public static let name = "Extraction"
}
