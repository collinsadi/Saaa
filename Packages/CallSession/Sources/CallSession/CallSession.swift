/// Session lifecycle state machine, global hotkey, and auto-stop heuristics.
public enum CallSessionModule {
    /// Module identity used in diagnostics and privacy-safe logs.
    public static let name = "CallSession"
}
